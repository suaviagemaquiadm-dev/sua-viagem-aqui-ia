# ############################################################################
# # 🛑 AÇÃO NECESSÁRIA: Seu deploy está falhando! Por favor, leia isto. 🛑      #
# ############################################################################
#
# O erro "Input required and not supplied: firebaseServiceAccount" significa
# que seu repositório do GitHub não tem as credenciais necessárias para fazer o deploy no
# Firebase. Este é um problema de CONFIGURAÇÃO, não um bug no código.
#
# Por favor, siga estes passos CUIDADOSAMENTE para corrigir permanentemente:
#
# ----------------------------------------------------------------------------
# 🎯 PASSO 1: VÁ PARA OS SEGREDOS DO GITHUB
# ----------------------------------------------------------------------------
#    1. No seu repositório GitHub, clique na aba "Settings".
#    2. Na barra lateral esquerda, navegue para "Secrets and variables" > "Actions".
#    3. Você verá uma seção chamada "Repository secrets".
#
# ----------------------------------------------------------------------------
# 🎯 PASSO 2: APAGUE SEGREDOS ANTIGOS/INCORRETOS (IMPORTANTE!)
# ----------------------------------------------------------------------------
#    - Procure por qualquer segredo chamado `FIREBASE_SERVICE_ACCOUNT` ou similar.
#    - Se encontrar, clique no ícone de lixeira 🗑️ para deletá-lo.
#    - Isso garante que você comece do zero e evite problemas de configuração antiga.
#
# ----------------------------------------------------------------------------
# 🎯 PASSO 3: CRIE O NOVO SEGREDO
# ----------------------------------------------------------------------------
#    1. Clique no botão "New repository secret".
#    2. No campo "Name", copie e cole EXATAMENTE isto:
#
#          FIREBASE_SERVICE_ACCOUNT
#
# ----------------------------------------------------------------------------
# 🎯 PASSO 4: PEGUE O VALOR DO SEGREDO NO FIREBASE
# ----------------------------------------------------------------------------
#    1. Abra seu projeto do Firebase: https://console.firebase.google.com/
#    2. Clique no ícone de engrenagem ⚙️ ao lado de "Visão geral do projeto" e selecione "Configurações do projeto".
#    3. Vá para a aba "Contas de serviço".
#    4. Clique no botão "Gerar nova chave privada".
#    5. Um arquivo `.json` será baixado para o seu computador.
#    6. Abra este arquivo com um editor de texto simples (como Bloco de Notas, VS Code, TextEdit).
#    7. Selecione TUDO no arquivo (Ctrl+A ou Cmd+A) e COPIE (Ctrl+C ou Cmd+C).
#       O conteúdo começa com `{` e termina com `}`.
#
# ----------------------------------------------------------------------------
# 🎯 PASSO 5: COLE O VALOR NO GITHUB
# ----------------------------------------------------------------------------
#    1. Volte para a página de criação de segredo do GitHub.
#    2. Na caixa de texto grande "Secret", COLE todo o conteúdo JSON que você acabou de copiar.
#    3. Clique no botão "Add secret".
#
# ----------------------------------------------------------------------------
# 🤔 ERROS COMUNS A EVITAR
# ----------------------------------------------------------------------------
#    ❌ NOME ERRADO: Usar `firebaseServiceAccount` ou qualquer outra variação. DEVE SER `FIREBASE_SERVICE_ACCOUNT`.
#    ❌ VALOR INCOMPLETO: Não copiar todo o conteúdo JSON, incluindo o `{` e `}`.
#    ❌ VARIÁVEL, NÃO SEGREDO: Criar uma "Action Variable" em vez de um "Action Secret". Segredos são criptografados.
#    ❌ REPOSITÓRIO ERRADO: Fazer isso nas configurações de um repositório diferente.
#
# Depois disso, o deploy deve funcionar no seu próximo push para a branch `main`.
# ############################################################################

name: Build, Test and Deploy to Firebase

on:
  push:
    branches:
      - main # Aciona em pushes para a branch principal
  pull_request:
    branches:
      - main # Aciona em pull requests para a branch principal

jobs:
  build_and_test:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: 'functions/package.json'

      - name: Install Functions Dependencies
        # Usa 'npm install' como fallback para ambientes sem package-lock.json
        run: npm install --prefix functions

      - name: Audit for Security Vulnerabilities
        run: npm audit --prefix functions --audit-level=high

      - name: Run Backend Tests
        run: npm test --prefix functions

  deploy:
    name: Deploy to Firebase
    needs: build_and_test # Depende do sucesso do job 'build_and_test'
    runs-on: ubuntu-latest
    # Apenas executa este job em pushes para a branch 'main', não em pull requests
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check for Firebase Service Account Secret
        env:
          FIREBASE_SA_SECRET: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: |
          if [ -z "$FIREBASE_SA_SECRET" ]; then
            echo "===================================================================="
            echo "====== ERRO CRÍTICO: SEGREDOS DO GITHUB NÃO CONFIGURADOS ======"
            echo "===================================================================="
            echo ""
            echo "O segredo 'FIREBASE_SERVICE_ACCOUNT' não foi encontrado."
            echo "Isto NÃO é um erro no código. Você precisa configurar os segredos"
            echo "nas configurações do seu repositório no GitHub."
            echo ""
            echo "=> POR FAVOR, SIGA AS INSTRUÇÕES NO TOPO DESTE ARQUIVO"
            echo "   ('.github/workflows/firebase-ci.yml') PARA RESOLVER."
            echo ""
            echo "===================================================================="
            exit 1
          fi

      # A ação `action-hosting-deploy` lida com o deploy de Functions e Hosting.
      - name: Deploy to Firebase
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          channelId: live
          projectId: gemini-cli-98f4a