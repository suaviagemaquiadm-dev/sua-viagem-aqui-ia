/**
 * ATENÇÃO: As chaves do Algolia foram removidas deste arquivo por razões de segurança.
 * A exposição de chaves de API no código do cliente é uma vulnerabilidade (OWASP A05).
 *
 * Para reativar a busca, as chaves devem ser gerenciadas de forma segura:
 * 1. Armazene-as como Segredos no GitHub.
 * 2. Utilize um framework de build (como Vite) para injetar as chaves como
 *    variáveis de ambiente durante o processo de deploy.
 * 3. Ou, crie uma Cloud Function que atue como um proxy seguro para as buscas no Algolia.
 */

// A inicialização do cliente Algolia foi movida para search.js e precisa ser
// alimentada com variáveis de ambiente seguras.

